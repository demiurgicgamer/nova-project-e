fastapi==0.111.0
uvicorn[standard]==0.29.0
langgraph==0.1.5
langchain-core==0.2.5
redis==5.0.4
asyncpg==0.29.0
pydantic==2.7.1
python-dotenv==1.0.1
httpx>=0.27.0

# LLM providers — all optional, install only what you need
# Claude (production)
anthropic==0.28.0

# Gemini (free tier — 1500 req/day, no card needed)
google-genai==1.10.0

# Groq (free tier — fast llama3/mixtral, no card needed)
groq==0.18.0
